# Brand source files

Not served by the site — kept here as the origin of the vector assets.

- `logo-art-deco.svg` — the full logo supplied by the client. `assets/monogram.svg`,
  `assets/wordmark.svg` and `assets/favicon.svg` were extracted from it.
- `logo-crest.png`, `logo-frame.png`, `logo-sunburst.png` — the logo variations supplied
  by the client (re-compressed).
